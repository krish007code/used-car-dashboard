name: 'used_car_dashboard'
version: '1.0.0'

profile: 'used_car_dashboard'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

vars:
  clean_data_path: "../clean/used_cars_clean.parquet"

models:
  used_car_dashboard:
    staging:
      +materialized: view
    marts:
      +materialized: table